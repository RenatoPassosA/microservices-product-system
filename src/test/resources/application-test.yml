spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/fallback
    username: fallback
    password: fallback
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  test:
    database:
      replace: none